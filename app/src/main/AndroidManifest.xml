<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso de internet para Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Permisos para fotos y cámara -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/pace_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pace_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pace">

        <!-- SplashActivity como página de inicio -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Pace.Splash"> <!-- tema launch screen -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity normal -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true" />

        <!-- ✅ AÑADIDO: ForgotPasswordActivity -->
        <activity
            android:name=".ui.login.ForgotPasswordActivity"
            android:exported="false" />

        <!-- ✅ AÑADIDO: SignUpActivity -->
        <activity
            android:name=".ui.signup.SignUpActivity"
            android:exported="false" />

        <activity
            android:name=".ui.tutorial.TutorialActivity"
            android:exported="false" />

        <!-- Añadir después de SignUpActivity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.habits.HabitSelectionActivity"
            android:exported="false" />

        <activity
            android:name=".ui.habits.DailyHabitsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.habits.HabitHistoryActivity"
            android:exported="false" />
        <activity
            android:name=".ui.achievements.MonthlyAchievementsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.achievements.AchievementUnlockActivity"
            android:theme="@style/AchievementUnlockTheme"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true" />
        <activity
            android:name=".ui.settings.AccountSettingsActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.settings.ActiveDevicesActivity"
            android:exported="false" />

        <activity
            android:name=".ui.circles.CirclesActivity"
            android:exported="false" />

        <!-- FileProvider para pasar la URI de la foto de alta resolución a la cámara -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".ui.circles.CircleChatActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <activity android:name=".ui.stats.StatsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.premium.PremiumActivity"
            android:exported="false" />

        <activity
            android:name=".ui.backup.CloudBackupActivity"
            android:exported="false" />

        <activity
            android:name=".ui.circles.DeepLinkActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Pace.Splash">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="pace"
                    android:host="join" />
            </intent-filter>
        </activity>

        <!-- Resuelve conflicto entre Firebase Analytics y AdMob por AD_SERVICES_CONFIG -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <!-- AdMob App ID — reemplazar con ID real de AdMob Console -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <service
            android:name=".receivers.PaceFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".receivers.ReminderReceiver"
            android:enabled="true"
            android:exported="false" />
        <receiver
            android:name=".receivers.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>

</manifest>